//// BFS %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
for(){
while(!visited[i])
    {
        dobfs(adjlist,i,visited);
    }
}


void dobfs(blah &adjlist, &node, &visited)
    {
        queue <ont>q;
        push nodeinto q
        visited[i]=1;
        while(!q.empty())
            {
                frontnode=pop
                cout<<frontnode
                for (auto i in adjlist[frrontnode])
                    if !visited
                    push into queue and visited =1
            }
    }


//DFS %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%5
